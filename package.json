{
  "name": "recipes-monorepo",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "bash ./dev.sh",
    "dev:web": "bun run --cwd web dev",
    "dev:convex": "bun run --cwd web dev:convex",
    "test:web": "bun run --cwd web test",
    "test": "bun run --cwd web test"
  }
}
